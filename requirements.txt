# Web application
streamlit

# Machine learning
torch
torchvision

# CLIP model (OpenAI) — also installs ftfy, regex, tqdm as sub-dependencies
git+https://github.com/openai/CLIP.git
ftfy
regex
tqdm

# Image and array processing
pillow
numpy<2
